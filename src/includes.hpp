#include <cctype>
#include <iostream>
#include <istream>
#include <list>
#include <sstream>
#include <string>
#include <fstream>
#include <variant>
#include <vector>
#include <bits/stdc++.h>
#include <filesystem>
#include <cassert>
#include <ranges>
#include <vector>

enum Window{
    MAINWINDOW,
    TASKWINDOW,
    TIMERWINDOW,
};


enum Priority{
    LOW,
    MEDIUM,
    HIGH,
};
